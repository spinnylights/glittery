<!DOCTYPE html>
<html>
<head>
  <title><%= site_title %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <!--[if lt IE 9]>
  <script src="<%= Rails.root.to_s + 
    'app/assets/javascripts/html5shiv.js' %>"</script>
  <![endif]-->
</head>
<body>
  <header id="header">
    <h1><%= site_title %></h1>

  <% unless artist_has_no_info? || request.fullpath == '/artist' %>
    <%= link_to "bio", artist_path %>
  <% end %>
  </header>

    <div class="flash_box">
      <% flash.each do |key, value| %>
        <div class="flash"><%= value %></div>
      <% end %>
    </div>

    <%= yield %>
  </body>
</html>
