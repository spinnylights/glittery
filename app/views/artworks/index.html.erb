<div id="gallery">
  <% @artworks.each do |artwork| %>
    <div class="art">
      <h3><%= artwork.name %></h3><br>
      <div class="image">
        <%= link_to(artwork) do %>
          <%= image_tag(artwork.image_url) %>
        <% end %>
        <br>
      </div>
    </div>
  <% end %>
</div>
