<div id="gallery">
  <% @artworks.each do |artwork| %>
    <div class="art">
      <h3><%= artwork.name %></h3><br>
      <div class="image">
        <%= image_tag(artwork.image_url) %><br>
      </div>
    </div>
  <% end %>
</div>
