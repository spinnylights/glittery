<h2>Change Password</h2>
<%= form_for current_admin, url: { action: 'update' }, 
  html: { class: "form", id: "change_password" } do |a| %>
  <%= render partial: 'shared/errors', locals: { instance: current_admin }  %>

  <%= a.label      :new_password %>
  <%= a.text_field :new_password %><br>

  <%= a.label      :password_confirmation, 'Confirm password' %>
  <%= a.text_field :password_confirmation %><br>
<div class="submit">
  <%= a.submit "Submit" %>
</div>
<% end %>
<%= link_to 'back', edit_admin_path %>
